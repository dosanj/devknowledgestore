import { html } from 'lit-html';

export default (props) => html`<div class="root-wrapper flex flex-column">
  <app-navbar></app-navbar>
  <app-landing-page class="flex-1"></app-landing-page>}
</div>`;
